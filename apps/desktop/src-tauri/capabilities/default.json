{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to do. Session commands are plain #[tauri::command]s and need no extra permission; anything beyond core:default has to be added here deliberately.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to do. App commands are plain #[tauri::command]s and need no extra permission; plugins (opener, updater) are only used from Rust, so the page gets none of their permissions. Anything beyond core:default and the title bar's own window controls has to be added here deliberately.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
